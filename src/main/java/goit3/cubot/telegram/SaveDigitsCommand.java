package goit3.cubot.telegram;

public class SaveDigitsCommand implements Command {
    public SaveDigitsCommand(String argument, long chatId) {
    }

    @Override
    public boolean execute() {
        return false;
    }
}
